@{
  Layout = "_Layout";
}

@using LibraryCatalog.Models;
@using System.Security.Claims
@model IEnumerable<LibraryCatalog.Models.Patron>

<h1>Patrons at our library:@User.Identity.Name</h1>
<hr />

@if (Model.Any())
{
  <ul>
    @foreach (Patron patron in Model)
    {
      <li>@Html.ActionLink($"{patron.PatronName}", "Details", new { id = patron.PatronId })</li>
    }
  </ul>
} 
else
{
  <h3>No titles have been added yet!</h3>
}

<p class="btn btn-dark">@Html.ActionLink("Add new patron", "Create")</p>

<p class="btn btn-dark">@Html.ActionLink("Home", "Index", "Home")</p>