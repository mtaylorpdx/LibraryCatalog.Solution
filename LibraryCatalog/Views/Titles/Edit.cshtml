@{
  Layout = "_Layout";
}

@model LibraryCatalog.Models.Title

<h2>Edit</h2>

<h4>Edit this title: @Html.DisplayFor(model => model.BookName)</h4>

@using (Html.BeginForm())
{
    @Html.HiddenFor(model => model.TitleId)

    @Html.LabelFor(model => model.BookName)
    @Html.EditorFor(model => model.BookName)

    @Html.Label("Select author")
    @Html.DropDownList("AuthorId")
@* 
    @Html.Label("Select patron")
    @Html.DropDownList("PatronId") *@

    <input type="submit" value="Save" />
}

<p>@Html.ActionLink("Back to list", "Index")</p>